<%= form_for(mark) do |f| %>
  <% if mark.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(mark.errors.count, "error") %> prohibited this mark from being saved:</h2>

      <ul>
      <% mark.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :quadro %>
    <%= f.text_field :quadro %>
  </div>

  <div class="field">
    <%= f.label :valor %>
    <%= f.text_field :valor %>
  </div>

  <div class="field">
    <%= f.label :cor %>
    <%= f.text_field :cor %>
  </div>

  <div class="field">
    <%= f.label :aula_id %>
    <%= f.text_field :aula_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
